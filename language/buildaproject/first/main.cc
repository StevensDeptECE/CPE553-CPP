#include <iostream>
#include <string>
#include <random>
using namespace std;

int main() {
	string hang = "hangman";
	string death = "";
	string secretWord = "patience";
	char c;

	for (;; ) {
		cout << "pick a letter";
		cin >> c;

		// check if c is in the secret word...
	}
	return 0;
}
