int main() {
	vector<int> a;

	//add numbers from 1 to 10 into a  1 2 3 ... 10
	//iterate through list and sum all the numbers using auto

	// double every element in the list using auto&
	
	//iterate and sum all the numbers using
	for (int i = 0; i < a.______; i++)
		cout << ______;

	// iterate using a const_iterator


	// print out the list backward using reverse_iterator
}
