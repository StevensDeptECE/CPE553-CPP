#include <iostream>

int main()
{
  for (int x = 10; x > 0; x--)
  {
    std::cout << x;
  }

  for (int x = 100; x >= 1; x /= 3)
  {
    std::cout << x;
  }
  /*
     a += b;    // a = a + b
     a -= b;    // a = a - b
     a *=
     a /=
     a %= 
     ...

  */
}