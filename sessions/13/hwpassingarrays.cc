int main() {
    int a[10];

    // sum should not be able to change a
    int z = sum(a, 10); // write a function sum where the first parameter is a pointer

    // clear changes a
    clear(a, 10); // write zero into every element of a
}

